SubForge — Development Roadmap
Last updated: v0.10.0 (complete) — next: v0.11.0
================================================================================

COMPLETED
--------------------------------------------------------------------------------
v0.4.0 — MKVToolNix Update
  - MKV Clean & Remux via mkvmerge
  - Per-block Keep button in Video Scan and Batch detail reports
  - Extract & Save standalone subtitle from video
  - MKVToolNix Settings dialog (path browse, saved to settings.json)
  - Sensitivity slider in Video Scan with live re-rendering
  - HTML detail reports in Video Scan and Batch

v0.5.0 — MP4 Remux Update
  - MP4 and M4V remuxing via ffmpeg (no extra dependencies)
  - Check for Updates button in status bar (opt-in, GitHub releases API)
  - Semantic versioning (v0.x.0 format)
  - pysubs2 added to requirements.txt

v0.6.0 — Interface Overhaul
  - Review + Report tabs merged into Single File tab
  - Sensitivity slider added to Single File tab
  - Tab-scoped controls (Dry run, Remove warnings, Open Folder inside Single File only)
  - File queue moved inside Single File tab (tabs always full width, no layout shift)
  - Show Full Report button in Batch restores full summary after drilling into a file
  - Video Scan folder label shows selected path after Add Folder
  - Extract & Save filename format: [video stem].[lang].[ext], SDH auto-detected
  - File queue shows color only (no stale count badges that don't reflect slider)
  - Toolbar gap fixed
  - Button height consistency fixed across all tabs
  - MKVToolNix notice updated to reflect MP4 support without mkvmerge

v0.7.0 — Subtitle Cleaning Options
  - core/cleaner_options.py — full content cleaning engine
  - Track title normalization on remux (encoder credits stripped, always on)
  - Global Settings dialog: Cleaning Options + Paths tabs, persists to settings.json
  - Settings button (⚙) top-right of toolbar
  - CLEAN OPT blue indicators in Single File, Batch, and Video Scan
  - Opts count in Single File bottom bar
  - Batch slider preserves detail view when moved
  - Opts-only tracks selectable in Video Scan
  - Cleaning actions shown in reports after save
  - File queue selection highlight fixed (neutral color, not status color)

v0.8.0 — Settings Expansion & Polish
  - App renamed to SubForge; entry point subforge.py; GitHub repo updated in updater.py
  - About tab in Settings: primary credit David R. Babcock, GitHub link, MIT license,
    Report an Issue button, KBlixt/subcleaner secondary credit
  - General tab in Settings: default sensitivity (global, all tabs), language selector
    (English + Spanish; additional languages via dropdown), Coming in v1.0.0 placeholders
  - String extraction: all Qt widget strings moved to gui/strings.py (flat dict)
    HTML report strings deferred to v0.9.0
  - Spanish translation: full STRINGS_ES dictionary, language persists to settings.json,
    restart prompt appears when language changes with one-click restart via os.execv
  - Column sort on Batch: QTableWidget with File/Ads/Opts/Warns columns,
    clickable headers, numeric sort, row colors preserved
  - Threshold labels unified across all tabs (removed rm≥N and TV/Movies annotations)
  - Sensitivity slider default now user-configurable in Settings General tab
  - Updater error dialog distinguishes 404 from network failure
  - Translation fallback: missing keys fall back to English, then show a
    visible placeholder directing user to Report an Issue

v0.9.0 — Packaging & Distribution
  - Full string extraction for HTML reports in Batch and Video Scan;
    14 language packs added (EN, ES, NL, HE, ID, PT, SV, FR, AR, ZH, HI, RU, TR, PL)
  - Session memory: last Batch folder, last Video Scan folder, and window
    size/position saved to settings.json and restored on next launch
  - First-run setup wizard: fires once on first launch, detects FFmpeg and
    MKVToolNix, shows green/red status per tool with download links
  - core/paths.py: centralised path resolution that works correctly both
    from source and inside a PyInstaller bundle
  - PyInstaller packaging: subforge.spec bundles the app into a standalone
    executable for Windows (macOS and Linux binaries deferred post-1.0)
  - Windows installer: Inno Setup script (installer/subforge_installer.iss)
    installs to Program Files, creates Start Menu shortcut, optional file
    associations for .srt/.ass/.vtt, includes uninstaller
  - GitHub Actions workflow: .github/workflows/build.yml auto-builds the Windows
    installer and attaches it to the GitHub release on tag push
    (macOS and Linux jobs written but disabled — pending hardware testing)
  - Double-click behaviour: launching with no arguments now opens the GUI
    instead of printing CLI help

v0.10.0 — OCR for Image-Based Subtitles ✓ COMPLETE
  - Image Subs tab: dedicated tab for scanning PGS and VOBSUB image-based
    subtitle tracks using Tesseract OCR
  - PGS path: raw .sup stream extracted via ffmpeg, parsed in pure Python
    with correct BT.601 limited-range YCbCr conversion and RLE decoder;
    bitmaps decoded and OCR'd in memory — no video decode required
  - VOBSUB path: ffmpeg 1fps overlay onto native subtitle canvas resolution
    (probed per-stream, not assumed from video dimensions), dedup, then OCR
  - Preprocessing pipeline: alpha-style detection (alpha-only vs colored text),
    appropriate background composite, bbox crop, 3x upscale
  - Tesseract garbage output filter ([PIL], isolated punctuation) applied to
    both PGS and VOBSUB paths
  - Detection engine runs on OCR output unchanged — same pipeline as text tracks
  - Save as .srt: OCR'd output saved as [stem].[lang].srt next to video
  - Remux into video: replace or keep-alongside original image track (MKV via
    mkvmerge; MP4/M4V via ffmpeg — keep-alongside not available for MP4)
  - Sensitivity slider on Image Subs tab, respects Settings > General default
  - Open in Image Subs: Video Scan hands off image-track files directly
  - Settings > Paths expanded: ffmpeg, ffprobe, Tesseract each have path entries
  - First-run wizard updated to surface Tesseract alongside FFmpeg and MKVToolNix
  - Language selector in first-run wizard — restarts app to apply fully
  - System language auto-detection on first launch
  - 14 language packs fully updated for all new Image Subs strings
  - Settings Save/Cancel buttons and sensitivity labels now translatable
  - What's New button in Settings > About — reads bundled CHANGELOG.md
  - pytesseract and Pillow bundled in Windows installer via PyInstaller
  - Experimental warning banner on Image Subs tab

PLANNED
--------------------------------------------------------------------------------

v0.11.0 — Whisper Audio Transcription
  - Whisper AI integration for local, offline subtitle generation from audio
      - Runs entirely on-device — no cloud, no API keys, no internet required
        consistent with SubForge's fully local philosophy
      - User selects a video file; Whisper transcribes audio to subtitle blocks;
        SubForge's cleaning engine runs on the output
      - SDH mode: generate with sound descriptions and speaker labels for
        deaf and hard of hearing viewers
      - Export as .srt/.ass or remux back into the video
      - Makes SubForge the only tool that both generates AND cleans subtitles
        in a single local application
  - Model selection (tiny/base/small/medium/large) with speed vs accuracy
    tradeoff explained in plain language
  - Language detection or manual language selection for transcription
  - New "Transcribe" tab (or integrated into Video Scan — TBD at design time)
  - Whisper binary/model path in Settings > Paths
  - faster-whisper or openai-whisper as backend (TBD — faster-whisper preferred
    for Windows compatibility and CUDA support)

v1.0.0 — Accessibility, Polish & Release
  - Light theme and high contrast theme (toggle in Settings)
  - Font size setting (small/medium/large)
  - Keyboard navigation audit — every action reachable without mouse
  - Screen reader compatibility — setAccessibleName/setAccessibleDescription
    on all major widgets (Windows Narrator, macOS VoiceOver)
  - Tooltip audit — every button has one
  - Windows installer appears behind other windows on launch — Inno Setup
    foreground focus issue. SetupMutex and WindowVisible both tried without
    success. May require a post-install launcher workaround.
  - Tesseract reads music note symbols (♪) as "J" — Tesseract has no glyph
    for ♪ in its training data and substitutes the nearest Latin character.
    Options: post-process OCR output replacing isolated "J" near known music
    context with ♪, or use a custom Tesseract training set. Low priority.
  - Image Subs sensitivity slider does not update track list colors — when
    slider is moved, the Track Detail pane re-renders correctly but the tree
    item color (red/orange/green) does not update to reflect the new threshold.
    Should call _update_tree_item_color() for all scanned tracks on slider change.
  - Language change dialog in Settings shows Yes/No buttons that are not
    translated — the dialog body correctly switches to the newly selected
    language but the QMessageBox standard buttons remain in the OS language.
    Fix by replacing QMessageBox.question with a custom dialog using
    STRINGS-based button labels in the newly selected language.
  - Additional translations — French, Dutch (detection profiles already exist)
  - GitHub Pages documentation site from README with tab screenshots
  - File association on macOS
  - Full test pass: Windows, macOS, Linux
  - Release candidate period before tagging 1.0.0
  - Final branding pass — every string, label, tooltip reviewed
  - Stop Scan button on Batch and Video Scan — graceful cancellation of an
    in-progress scan that preserves results collected so far and does not
    corrupt the report. The current Clear button is not reliable for this.
    Requires a cancellation flag on the worker thread.
  - Bottom status bar currently only reflects Single File tab activity — Batch,
    Video Scan, and Image Subs all manage their own internal status labels and
    do not update the app-level bar. Wire tab-switching to sync the bottom bar
    with the active tab's last status, or redesign to use a single shared bar.
  - Translated READMEs — once the feature set is stable, generate per-language
    README files (README.es.md, README.fr.md, etc.) in a docs/ folder and link
    them from the top of the main README. AI-generated to match the 14 language
    packs. Maintain once, rarely update after 1.0.


POST-1.0 — FUTURE FEATURES

  MACOS AND LINUX BINARIES
  - Dropped from v0.9.0 scope — Windows installer alone was complex enough
    to validate. Source code remains cross-platform.
  - macOS: requires physical hardware to test, code signing complexity
  - Linux: can be tested via VM — candidate for a minor post-1.0 release
  - GitHub Actions workflow already has macOS/Linux jobs written and ready;
    just needs testing and re-enabling when the time comes
--------------------------------------------------------------------------------
These are real planned features, not wishlist items. Priority order TBD.

  SUBTITLE FORMAT CONVERSION
  - SRT ↔ ASS ↔ VTT conversion within the app
  - Batch format conversion across a library folder

  ADDITIONAL TRANSLATIONS
  - Community contribution system via GitHub for additional languages beyond French/Dutch

  ANDROID
  - BeeWare/Briefcase port
  - Genuinely complex, not a near-term priority

  AUTO-UPDATE
  - Download and apply updates in-app rather than linking to releases
  - Security implications need careful review before implementing

  PLUGIN SYSTEM
  - User-written detection logic in Python beyond regex
  - For edge cases the regex engine cannot handle


DECISIONS & STANDING RULES
--------------------------------------------------------------------------------
  - Check for Updates stays in status bar. Opt-in and visible. Non-negotiable.
  - Settings is a dialog opened from toolbar button, NOT a tab.
  - Settings button lives on far right of toolbar (⚙ Settings).
  - No emojis on buttons except the gear icon on Settings.
  - Sensitivity slider: no recommendations made — user-judgment based. Default configurable in Settings.
  - Version format: v0.x.0 through 1.0.0, then semantic (1.0.1, 1.1.0 etc.)
  - Remux supports MKV (mkvmerge) and MP4/M4V (ffmpeg). No mkvmerge for MP4.
  - Extract & Save filename: [video stem].[lang].[ext] e.g. Movie.eng.srt
    SDH tracks: Movie.eng.sdh.srt
  - File queue in Single File shows color only, no stale count badges.
  - SubForge is an accessibility tool — SDH removal and any feature that
    degrades the HoH experience must carry a visible warning and must never
    be enabled by default. Always default to the HoH-friendly option.
  - Track title normalization during remux is always on — no option.
    Encoder credits in track titles serve no user and actively hurt
    HoH users navigating subtitle menus.
  - Cleaning options are global (settings.json), not per-tab.
  - mkvmerge path lives in Settings > Paths, not in Video Scan tab.
